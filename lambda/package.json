{
  "name": "@awsworkshop/lambda",
  "version": "1.0.0",
  "description": "Lambda functions of the application",
  "main": "src/chat/conversation.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=16.0"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.363.0",
    "@types/aws-lambda": "^8.10.119",
    "node_module": "^0.0.0",
    "openai": "^3.3.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^20.3.3",
    "jest": "^29.6.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
